
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build Git Pipe'
            }
        }
        stage('Test') {
            steps {
                echo 'Test Git Pipe'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy Git Pipe'
            }
        }
    }
}
